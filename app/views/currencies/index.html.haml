%p#notice= notice
%h1 Currencies
%table.table.table-striped.listing_table
  %thead
    %tr
      %th Currency name
      %th Iso code
      %th Symbol
      %th Sorder
      %th Actions
  %tbody
    - @currencies.each do |currency|
      %tr
        %td= currency.currency_name
        %td= currency.iso_code
        %td= currency.symbol
        %td= currency.sorder
        %td
          = link_to 'Show', currency
          |
          = link_to 'Edit', edit_currency_path(currency)
          |
          = link_to 'Destroy', currency, method: :delete, data: { confirm: 'Are you sure?' }