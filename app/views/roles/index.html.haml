%h1 Roles
= link_to 'New Role', new_role_path, class: 'btn btn-primary pull-right'
%table.table.table-striped.table-hover
  %thead
    %tr
      %th Name
      %th Actions
  %tbody
    - @roles.each do |role|
      %tr
        %td= role.name
        %td
          = link_to 'Edit', edit_role_path(role), class: 'btn btn-sm btn-primary'
          = link_to 'Delete', role, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger'
%br/
